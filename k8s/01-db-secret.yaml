# Replace the placeholder values below (or create with `kubectl create secret generic`
# from the CLI instead of committing real credentials to yaml).
apiVersion: v1
kind: Secret
metadata:
  name: db-secret
  namespace: three-tier-login
type: Opaque
stringData:
  DB_HOST: "mysql -h three-tier-db.cxigkw8m697j.ap-south-1.rds.amazonaws.com"
  DB_USER: "admin"
  DB_PASSWORD: "root1234"
  DB_NAME: "three-tier-db"
  DB_PORT: "3306"
  JWT_SECRET: "change-this-secret"
